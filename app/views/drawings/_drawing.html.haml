.li
    .row
        .col-md-8
            .drawing-container
                .drawing-image
                    -if drawing.attachment.present?
                        %img{:src => drawing.attachment_url}
                .drawing-info
                    %p= drawing.title
